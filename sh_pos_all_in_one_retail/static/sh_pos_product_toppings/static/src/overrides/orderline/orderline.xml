<templates id="template" xml:space="preserve">
    <!-- Force-disable any animation on all POS orderlines -->
    <t t-name="sh_pos_product_toppings.OrderlineNoAnim" t-inherit="point_of_sale.Orderline" t-inherit-mode="extension">
        <xpath expr="//li[hasclass('orderline')]" position="attributes">
            <attribute name="style">animation: none !important; transition: none !important; opacity: 1 !important;</attribute>
        </xpath>
    </t>
  
    <!-- added this code in wh stock module -->
    <!-- <t t-name="Orderline" t-inherit="point_of_sale.Orderline" t-inherit-mode="extension">
        <xpath expr="//ul[hasclass('info-list')]" position="after">
            <t t-if="line.is_has_topping">
                <div class='topping-item' t-if='line.Toppings'>
                    <t t-foreach="line.Toppings" t-as="topping" t-key="topping.id">
                        <div class="sh_topping_line_content">
                            <div class="sh_toppings_description">
                                <div style="padding: 0px 5px" class="fa fa-trash" t-att-data-line="topping.id" t-on-click.stop="() => this._clickRemoveLine(topping.id)"></div>
                                <t t-if="topping">
                                    <div class="sh_product_name">
                                        <t t-esc='topping.full_product_name' />
                                    </div>

                                    <em style="margin-left: 5px;">
                                        <t t-esc="topping.quantityStr" />
                                    </em>

                                    <div style="margin-left: 5px;">
                                        <t t-esc="topping.unit" />
                                    </div>
                                    <span style="padding: 0px 5px"> at </span>
                                    <div class="addon-orderline-price">
                                        <t t-esc="env.utils.formatCurrency(topping.price_unit)" />
                                    </div>
                                    <span style="padding: 0px 5px"> / </span>
                                    <t t-esc="topping.unit" />
                                </t>
                            </div>
                            <div style="margin-left: 15px; float: right;">
                                <t t-esc="env.utils.formatCurrency(topping.price_subtotal_incl)" />
                            </div>
                        </div>
                    </t>
                </div>
            </t>
        </xpath>
    </t> -->

    <!-- Keep topping lines hidden on receipt by guarding the Orderline component -->
    <t t-name="OrderReceipt" t-inherit="point_of_sale.OrderReceipt" t-inherit-mode="extension">
        <xpath expr="//Orderline" position="attributes">
            <attribute name="t-if">!line.is_topping</attribute>
        </xpath>
    </t>

    <!-- Normal POS receipt custom lines: Qty first, then product; right column shows price (excl. tax). -->
    <t t-name="sh_pos_product_toppings.OrderReceiptLines" t-inherit="point_of_sale.OrderReceipt" t-inherit-mode="extension">
        <!-- Hide the default order lines rendering -->
        <xpath expr="//OrderWidget" position="attributes">
            <attribute name="style">display:none</attribute>
        </xpath>

        <!-- Inject our custom compact lines before the hidden widget -->
        <xpath expr="//OrderWidget" position="before">
            <div class="pos-receipt-left-padding">
                <div>
                    <span>Description</span>
                    <span class="pos-receipt-right-align">Price</span>
                </div>
                <t t-foreach="props.data.orderlines" t-as="line" t-key="line.id">
                    <t t-if="!line.is_topping">
                        <t t-set="price_excl" t-value="line.priceWithoutTax || line.price_subtotal || line.price_subtotal_excl || 0"/>
                        <div>
                            <span><t t-esc="line.qty" /> </span>
                            <span><t t-esc="line.productName" /></span>
                            <span class="pos-receipt-right-align">
                                <t t-if="price_excl">
                                    <t t-esc="props.formatCurrency(price_excl)" />
                                </t>
                                <t t-else="">
                                    <t t-esc="line.price" />
                                </t>
                            </span>
                        </div>
                        <t t-if="line.customerNote">
                            <div class="pos-receipt-left-padding pos-receipt-customer-note">
                                <small><i class="fa fa-pencil-square-o">&#160;<t t-esc="line.customerNote"/></i></small>
                            </div>
                        </t>
                        <t t-if="line.is_has_topping &amp;&amp; line.Toppings &amp;&amp; line.Toppings.length">
                            <div class="pos-receipt-left-padding">
                                <t t-foreach="line.Toppings" t-as="topping" t-key="topping.id">
                                    <small><div>- 
                                        <t t-if="topping.full_product_name">
                                            <t t-esc="topping.full_product_name" />
                                        </t>
                                        <t t-else="">
                                            <t t-if="topping.product">
                                                <t t-esc="topping.product.display_name" />
                                            </t>
                                        </t>
                                    </div></small>
                                </t>
                            </div>
                        </t>
                    </t>
                </t>
            </div>
        </xpath>
    </t>

</templates>
